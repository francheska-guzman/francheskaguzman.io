import React, { Component } from 'react';

class Footer extends Component {
  render() {
    return (
      <div className="container">This is Footer component.</div>
    );
  }
}

export default Footer;